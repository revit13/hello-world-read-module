apiVersion: app.m4d.ibm.com/v1alpha1
kind: M4DApplication
metadata:
  name: hello-world-read-module-test
spec:
  selector:
   workloadSelector:
     matchLabels: {
       app: notebook
     }
  appInfo:
    purpose: Fraud Detection
    role: Data Owner
  data:
    - dataSetID: "default/data-csv"
      requirements:
        interface:
          protocol: s3
          dataformat: csv